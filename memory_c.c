/* checks inside memory
 * will be helpful to check 
 * inside a certain position of memory in your data set
 * Find out how you can generalize the array in this code
 */

#include <stdio.h>

int main(void)
{
    int numbers[] = {1, 2, 3, 4, 5};

    printf("%d\n", numbers[4]);

    return 0;
}