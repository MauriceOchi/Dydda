#include <stdio.h>
#include <stdbool.h>

int main(void)
{
    enum gender {male, female};
    enum gender myGender = male;
    enum gender anotherGender = female;

    bool isMale = (myGender == anotherGender);
    //find out how to print bool
    printf("%")


    return 0;
}